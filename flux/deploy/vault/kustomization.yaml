apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: vault
  namespace: flux-system
spec:
  interval: 5m
  path: "./valut"
  prune: true
  sourceRef:
    kind: GitRepository
    name: k3s-gitops
    namespace: flux-system
  validation: client
  configMapGenerator:
    - name: vault-values
      files:
        - values.yaml
